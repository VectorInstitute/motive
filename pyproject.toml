[project]
name = "motive"
version = "0.0.1"
description = "Answer 'why did the agent do that?' with saliency, rationale, and counterfactuals"
readme = "README.md"
requires-python = ">=3.11"
license = { file = "LICENSE" }
dependencies = []

[build-system]
requires = ["hatchling>=1.29.0"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/motive"]
